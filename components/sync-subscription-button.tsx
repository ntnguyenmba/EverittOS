'use client';

type SyncSubscriptionButtonProps = {
  onSynced?: (plan: string, status: string) => void;
};

export function SyncSubscriptionButton(props: SyncSubscriptionButtonProps) {
  void props;
  return null;
}
