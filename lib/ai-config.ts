/**
 * @deprecated Import from '@/lib/ai/config' instead.
 * Kept for backward compatibility with existing imports.
 */
export {
  aiConfigured,
  aiModel,
  aiProviderDisplayName,
  aiProviderId,
  getActiveAiProvider,
  getActiveAiProviderInfo,
  openAiApiKey,
  openAiConfigured,
  openAiModel,
  resolveAiProviderId
} from '@/lib/ai/config';
