'use client';
import Link from 'next/link';import{useCallback,useEffect,useMemo,useState}from'react';import{useRouter}from'next/navigation';import{AppShell}from'@/components/app-shell';import{useAppFeedback}from'@/components/feedback/use-app-feedback';import{PlanLockedMessage}from'@/components/plan-locked-message';import{billingUpgradeHref}from'@/lib/nav-access';import{CATCH_PRODUCT_NAME,CATCH_STAGES,buildCatchSnapshot,type CatchRecord}from'@/lib/catch-engine';import{customerDisplayName,type CustomerRecord}from'@/lib/customer-record';import{normalizeLeadStage}from'@/lib/lead-pipeline';import{canAccessFeature}from'@/lib/plan-access';import{normalizePlan,type EverittosPlan}from'@/lib/everittos-plans';import{fetchOrganizationContext}from'@/lib/organization';import{isManagerRole,normalizeRole,type UserRole}from'@/lib/roles';import{ensureWorkspaceForSave}from'@/lib/workspace-client';import{supabase}from'@/lib/supabase';import styles from'./catch.module.css';
type JobRow={id:string;title?:string|null;status?:string|null;updated_at?:string|null;created_at?:string|null};type BookingRow={id:string;status?:string|null;updated_at?:string|null;created_at?:string|null;customer_name?:string|null;name?:string|null;email?:string|null;phone?:string|null};
export default function CatchPage(){const router=useRouter(),feedback=useAppFeedback();const[plan,setPlan]=useState<EverittosPlan>('free'),[role,setRole]=useState<UserRole>('owner'),[loading,setLoading]=useState(true),[records,setRecords]=useState<CatchRecord[]>([]),[copiedId,setCopiedId]=useState<string|null>(null);const allowed=canAccessFeature(plan,'catchGrowth'),snapshot=useMemo(()=>buildCatchSnapshot(records),[records]);
const load=useCallback(async()=>{setLoading(true);const{data:{user}}=await supabase.auth.getUser();if(!user){router.push('/login');return}const{data:profile}=await supabase.from('profiles').select('plan, role').eq('id',user.id).maybeSingle();const org=await fetchOrganizationContext(user.id);setPlan(normalizePlan(profile?.plan));setRole(normalizeRole(org?.role||profile?.role));const workspace=await ensureWorkspaceForSave(user.id);const organizationId=workspace.ok?workspace.workspace?.organizationId:org?.organizationId;let customerQuery=supabase.from('customers').select('id, company_name, contact_name, email, phone, record_type, pipeline_stage, updated_at, created_at').order('updated_at',{ascending:false}).limit(200),jobQuery=supabase.from('jobs').select('id, title, status, updated_at, created_at').order('updated_at',{ascending:false}).limit(200),bookingQuery=supabase.from('bookings').select('id, status, updated_at, created_at, customer_name, name, email, phone').order('updated_at',{ascending:false}).limit(100);if(organizationId){customerQuery=customerQuery.eq('organization_id',organizationId);jobQuery=jobQuery.eq('organization_id',organizationId);bookingQuery=bookingQuery.eq('organization_id',organizationId)}else{customerQuery=customerQuery.eq('user_id',user.id);jobQuery=jobQuery.eq('user_id',user.id)}const[customersRes,jobsRes,bookingsRes]=await Promise.all([customerQuery,jobQuery,bookingQuery]);const next:CatchRecord[]=[];for(const row of(customersRes.data||[])as CustomerRecord[]){const leadLike=row.record_type==='lead';next.push({id:row.id,kind:leadLike?'lead':'customer',name:customerDisplayName(row),stage:leadLike?'inquiry':'customer',status:leadLike?normalizeLeadStage(row.pipeline_stage):'customer',email:row.email,phone:row.phone,updatedAt:row.updated_at,createdAt:row.created_at,href:leadLike?`/leads/${row.id}`:`/customers/${row.id}`})}for(const row of(jobsRes.data||[])as JobRow[])next.push({id:row.id,kind:'job',name:row.title||'Untitled job',stage:'job',status:row.status,updatedAt:row.updated_at,createdAt:row.created_at,href:`/jobs/${row.id}`});if(!bookingsRes.error)for(const row of(bookingsRes.data||[])as BookingRow[])next.push({id:row.id,kind:'booking',name:row.customer_name||row.name||'Booking request',stage:'booking',status:row.status||'pending',email:row.email,phone:row.phone,updatedAt:row.updated_at,createdAt:row.created_at,href:'/bookings'});setRecords(next);setLoading(false)},[router]);useEffect(()=>{void load()},[load]);async function copyMessage(id:string,message:string){try{await navigator.clipboard.writeText(message);setCopiedId(id);feedback.success('Reply copied.')}catch{feedback.error('Could not copy the reply.')}}
return <AppShell plan={plan} role={role}><div className={styles.page}><header className={styles.hero}><div><p className={styles.kicker}>{CATCH_PRODUCT_NAME} · Enterprise</p><h1>Know who needs your attention next.</h1><p className={styles.intro}>Scout watches your inquiries, bookings, customers and jobs, then puts the next important follow-up in one simple list. Nothing is sent without you.</p></div>{isManagerRole(role)?<div className={styles.heroActions}><Link className="btn btn-primary" href="/leads/new">Add inquiry</Link><Link className="btn" href="/bookings">View bookings</Link></div>:null}</header><section className={styles.explainer} aria-label="How Scout works"><div><span>1</span><strong>Scout checks</strong><p>Your inquiries, bookings and jobs.</p></div><div><span>2</span><strong>You review</strong><p>See who is waiting and why it matters.</p></div><div><span>3</span><strong>Scout writes</strong><p>Get a ready-to-use follow-up message.</p></div><div><span>4</span><strong>You decide</strong><p>Open the record or copy the reply.</p></div></section>{!allowed?<div className={styles.lock}><PlanLockedMessage feature="Scout" requiredPlan="Enterprise"/><p>Enterprise includes Scout, your daily follow-up list for inquiries, bookings and completed work.</p><Link className="btn btn-primary" href={billingUpgradeHref('enterprise','Scout')}>Upgrade to Enterprise</Link></div>:<><section className={styles.metrics} aria-label="Scout snapshot"><article className={styles.metric}><span>Need a reply</span><strong>{loading?'—':snapshot.agingInquiries}</strong><em>Inquiries waiting 24+ hours</em></article><article className={styles.metric}><span>Follow-ups due</span><strong>{loading?'—':snapshot.followUpsDue}</strong><em>Bookings and completed work</em></article><article className={styles.metric}><span>Conversion</span><strong>{loading?'—':`${snapshot.conversionRate}%`}</strong><em>Inquiries becoming customers or jobs</em></article></section><section className={styles.pipeline}><div className={styles.sectionHeading}><div><p className={styles.eyebrow}>Your work</p><h2>Where everything stands</h2></div><p>Open any stage to see the records behind the number.</p></div><div className={styles.funnel}>{CATCH_STAGES.map(stage=><Link key={stage.id} href={stage.href} className={styles.stage}><span>{stage.label}</span><strong>{loading?'—':snapshot.counts[stage.id]}</strong><em>{stage.hint}</em></Link>)}</div></section><section className={styles.actionsSection}><div className={styles.sectionHeading}><div><p className={styles.eyebrow}>Start here</p><h2>Who needs attention</h2></div><p>Scout puts the most useful next actions first.</p></div><div className={styles.actionList}>{loading?<p className={styles.empty}>Checking your work…</p>:null}{!loading&&snapshot.actions.length===0?<div className={styles.empty}><strong>You are caught up.</strong><p>When an inquiry, booking or finished job needs attention, Scout will put it here.</p></div>:null}{snapshot.actions.map(action=><article key={action.id} className={styles.action}><div className={styles.actionCopy}><span className={styles.actionReason}>{action.reason}</span><h3>{action.title}</h3><p className={styles.label}>Suggested reply</p><blockquote>{action.message}</blockquote></div><div className={styles.actionButtons}><Link className="btn" href={action.href}>View details</Link><button type="button" className="btn btn-primary" onClick={()=>void copyMessage(action.id,action.message)}>{copiedId===action.id?'Copied':'Copy reply'}</button></div></article>)}</div></section><section className={styles.note}><strong>You stay in control.</strong><p>Scout does not automatically text or email customers. It reads your existing EverittOS records, points out what needs attention and prepares the message for you.</p></section></>}</div></AppShell>}
